<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <Style TargetType="Window" 
               x:Key="WindowTheme">
        <Setter Property="Background" Value="#20104e"/>
    </Style>

    <Style TargetType="RadioButton" 
               x:Key="RadioButtonTheme">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid VerticalAlignment="Stretch"
                              HorizontalAlignment="Stretch"
                              Background="{TemplateBinding Background}">
                            <TextBlock Text="{TemplateBinding Property=Content}"
                                       VerticalAlignment="Center"
                                       Margin="50,0,0,0"
                                       Foreground="White"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="white"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style.Setters>

        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#2c1866"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TextBlockInListBox" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="Black"/>
    </Style>

    <Style TargetType="TextBlock"
           x:Key="TextBlockTheme">
        <Setter Property="Foreground" Value="Black"/>
    </Style>

    <Style TargetType="TextBlock"
           x:Key="TextBlockThemeWithoutBorder">
        <Setter Property="Foreground" Value="White"/>
    </Style>
    
</ResourceDictionary>